import {getKeyWithPrefix} from "../utils";

export const PREFIX = 'react'
export const CATEGORY_KEY = getKeyWithPrefix('category', PREFIX)
export const RECORD_KEY = getKeyWithPrefix('record', PREFIX)
